--- a/index.js	2026-07-17 07:07:30.630429400 +0000
+++ b/index.js	2026-07-17 07:07:30.645429066 +0000
@@ -20,6 +20,12 @@
 // options.edit = 'wss://www.yourcodeinstance.com/?id=projectname'
 options.release = process.argv.includes('--release');
 
+// Run as a single foreground process (Total.js "release" mode). Without this,
+// the default debug launcher forks a detached worker and the supervisor
+// exits, which is incompatible with single-process hosts such as Wasmer
+// Edge. The port is taken from the PORT environment variable.
+options.watcher = false;
+
 // Service mode:
 options.servicemode = process.argv.includes('--service') || process.argv.includes('--servicemode');
 // options.servicemode = 'definitions,modules,config';
--- a/package.json	2026-07-17 07:07:30.631429377 +0000
+++ b/package.json	2026-07-17 07:07:30.645429066 +0000
@@ -8,7 +8,7 @@
       "url": "https://github.com/totaljs/cms"
   },
   "dependencies": {
-    "total5": "latest"
+    "total5": "0.0.18"
   },
   "scripts": {
     "start": "node index.js 8000"
