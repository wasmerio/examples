---
kind: wasmer.io/App.v0
name: wasmer-edge-umami-sample
package: wasmer-tests/wasmer-edge-umami-sample
env:
  # Point this at your PostgreSQL database (required).
  DATABASE_URL: postgres://user:password@your-db-host:5432/umami
  # Set this to a long random string (required).
  APP_SECRET: change-me
