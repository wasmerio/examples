{
  "name": "umami-edgejs",
  "version": "3.2.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('js-umami ships a prebuilt .next/standalone (see EDGEJS-NOTES.md); nothing to build')\"",
    "start": "HOSTNAME=0.0.0.0 node .next/standalone/server.js"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "prisma": "^7.8.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/engines",
      "prisma"
    ]
  }
}
